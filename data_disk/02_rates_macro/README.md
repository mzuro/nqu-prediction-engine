# 02_rates_macro

Daily rates/macro context inputs land here:

- FRED series (e.g. DGS10, T10Y2Y, SOFR)
- Treasury yield proxies / TNX
- VIX (and optionally VXN / MOVE)

Prefer one CSV per series or a tidy long-format panel with `date,series,value`.
