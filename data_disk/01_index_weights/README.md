# 01_index_weights

Pin a **dated** NDX weights CSV here (e.g. `ndx_weights_YYYY-MM-DD.csv`).

Weights drift with price and rebalances. Do **not** treat a stale list as eternal truth — refresh at build time and keep the as-of date in the filename or a header column.
